services:
  mtg-panel:
    build: .
    container_name: mtg-panel
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - ./ssh_keys:/ssh_keys:ro
    environment:
      - PORT=3000
      - DATA_DIR=/data
      - AUTH_TOKEN=${AUTH_TOKEN:-changeme}
